import Link from "next/link";
import { QRCodeSVG } from "qrcode.react";
export default async function Confirmation({params}:{params:Promise<{bookingId:string}>}){const{bookingId}=await params;return <main className="form-page"><div className="container" style={{maxWidth:760}}><div className="form-card" style={{textAlign:"center"}}><div className="ai-orb">✓</div><span className="kicker" style={{display:"block",marginTop:20}}>Réservation démo confirmée</span><h1>Votre voyage est prêt.</h1><p>Référence <b>{bookingId}</b></p><div style={{display:"inline-block",padding:18,background:"white",border:"1px solid var(--border)",borderRadius:18,margin:20}}><QRCodeSVG value={JSON.stringify({bookingReference:bookingId,status:"DEMO"})} size={170}/></div><div className="demo">Ce billet est une démonstration et ne permet pas d’embarquer. Vérifiez et réservez auprès de l’opérateur officiel.</div><Link href={`/bookings/${bookingId}`} className="primary-btn button-link">Voir ma réservation</Link><Link href="/" className="secondary-btn button-link">Retour à l’accueil</Link></div></div></main>}
